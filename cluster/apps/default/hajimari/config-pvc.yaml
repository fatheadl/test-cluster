---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: hajimari-config
  namespace: default
  
spec:
  accessModes:
    - ReadWriteOnce
  #storageClassName: freenas-nfs-csi
  resources:
    requests:
      storage: 128Mi
